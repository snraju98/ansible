- name: demo on play level variables
  hosts: localhost
  connection: local  # it will not ask for user name and password
  vars:
    COURSE: "Ansible"
    TRAINER: "sivakumar"
    DURATION: "150HRS"
  tasks:
  - debug:
      msg: "hi,  iam learning {{ COURSE }}, trainer is {{ TRAINER }},Duration is {{ DURATION }}"