- name: install packages
  hosts: frontend
  become: yes
  tasks:
  - name: install packages
    package:
      name: "{{ item }}"
      satate: present
    loop:
    - nginix
    - mysql
    - mongodb-mongosh
    - git